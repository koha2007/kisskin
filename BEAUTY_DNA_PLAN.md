# "나만의 메이크업 찾았다" 실행 계획 (P0 → P3)

> 기능명(확정): **나만의 메이크업 찾았다**
> 라우트: `/tools/beauty-dna/` (+ `/en/tools/beauty-dna/`) — URL slug 은 `beauty-dna` 유지, 표시명만 위 문구
> 작성: 2026-09-10 · 이 문서는 **합의용 계획서**다. 합의 후 P0 첫 슬라이스부터 커밋 단위로 들어간다.
> 디자인 계열: 힐다(크림 + 버밀리언 `#d8503c`) — `/tools` 흐름이므로

---

## 0. 한 줄 정의

무료도구 4종(퍼스널컬러 · 얼굴형 · 향수 · 메이크업 MBTI)의 결과를 한곳에 모아
**나에게 딱 맞는 메이크업 루틴과 살 수 있는 제품**을 뽑아 주고, 그 결과를
셀카 없이 **모델 얼굴 1장**으로 시각화한다.

- **핵심 성과 = 제품 매칭(어필리에이트 수익).** 생성 모델 이미지는 결과 시각화지 목적이 아니다.
- 기존 `/analysis/`(셀카 업로드 → 9룩) 와 **완전히 별개 제품**. 병존한다.

---

## 1. 사용자 흐름 (운영자 정의 6단계)

| 단계 | 내용 | 지금 있는 것 | 새로 만들 것 |
|---|---|---|---|
| 1. 회원가입 | 로그인 사용자만 생성 가능 | Supabase 인증 | 없음 |
| 2. 4종 완료 → 메이크업 1회 무료 | 무료도구 4개를 모두 끝내야 생성 잠금 해제 | 크레딧 "첫 1회 무료" 로직 | 각 도구 완료 여부 `localStorage` 저장 + 4/4 게이트 |
| 3. 4종 결과 취합 | 4개 결과를 한 페이지에 정리 | 도구별 유형 추천 데이터(톤·립색·컨투어·추천룩·제품+어필리에이트) | `/tools/beauty-dna/` 신규 페이지 · `useBeautyDna()` 훅 · 4개 추천맵 병합 규칙 |
| 4. 취합 결과로 메이크업 생성 | 정리된 4종 → 생성 트리거 | — | 취합 페이지의 생성 버튼(로그인 + 4/4 + 크레딧 게이트) |
| 5. 모델 얼굴 생성 + 4종 결과 메이크업 적용 | 유저 셀카 아님. 모델 얼굴 + DNA 기반 메이크업 | `functions/api/makeup-edit.ts`(image-to-image · 비용가드 · 로그인 · 크레딧 내장) · `scripts/gen-look-models.mjs`(우리 모델 사전제작 패턴) | 베이스 얼굴 5종 + 4종 코드 → 메이크업 파라미터 결정적 변환 + `일러스트 · 실제 인물 아님` 라벨 |
| 6. 웹 위치·레이아웃·디자인 | — | `BentoGrid`·`IdentityCard`·`ShareBar`·`QuizScreen`·`ToolCard`·`RegionToggle` 재사용 | §4 |

---

## 2. 확정된 결정 4건

### 2-1. 무료 크레딧 — 총 2회, 별도 풀

| 경로 | 입력 | 무료 | 게이트 |
|---|---|---|---|
| ① 셀카 메이크업 (`/analysis/`) | 사용자 사진 | 1회 | 로그인 |
| ② 나만의 메이크업 찾았다 (신규) | 셀카 없음 | 1회 | 로그인 **+ 무료도구 4/4 완료** |

②의 "4종 완료" 조건이 사실상 남용 차단이다(4개 진단을 다 끝내야 두 번째 무료가 열린다).
생성 1건당 이미지 API 과금이 붙지만 이 진입 장벽으로 수용 가능.

### 2-2. 베이스 얼굴 5종

얼굴형별(oval / round / square / oblong / heart) 모델 얼굴을 **사전 제작해 리포에 커밋**.
유저당 image-to-image 1회만 실행(베이스는 재생성하지 않는다). 얼굴형 결과가 얼굴 구조로 반영됨.
- 스크립트: `scripts/gen-dna-portraits.mjs` 신설 — `gen-look-models.mjs` 의 MODELS dict 패턴 재사용.
- 산출물: `public/dna/base-{oval|round|square|oblong|heart}.webp` (3:4).

### 2-3. 이름 — "나만의 메이크업 찾았다"

4개 도구가 "퍼스널컬러 진단 / 얼굴형 진단…" 형식이므로 자매 항목으로 이 문구.
부제: *4가지 진단으로 딱 맞는 메이크업과 제품*.

### 2-4. 기존 `/analysis/` 와 관계 — 병존

셀카 업로드 흐름은 그대로. 신규는 "셀카 없이 내 DNA를 입은 모델" 경로.
포지셔닝 문구에서 "AI가 당신 얼굴을 생성" 류 금지 → **유형 조합 아키타입 합성**.

---

## 3. 데이터 / 상태

- `localStorage['kissin:dna:v1'] = { personalColor, faceShape, perfume, mbti, portraitUrl?, updatedAt }`
- 신규 훅 `useBeautyDna()` → `{ dna, progress, missing[], complete }`
- 4개 결과 페이지에 **완료 시 1줄 저장** 이펙트 추가 (현재 도구 결과는 저장 안 됨 — sessionStorage 로 잠깐 썼다 지움)
- 유형 개수: 퍼스널컬러 4 · 얼굴형 5 · 향수 6 · MBTI 16 → 조합 1,920 → **사전 렌더 불가**, 그래서 §2-1 처럼 유저당 1회 온디맨드
- 공유 URL `?c=autumn-heart-woody-enfp` → 로그인·localStorage 없이도 리드아웃/루틴/제품 렌더(16Personalities식 "너도 해봐" 루프)
- `?c=` 페이지는 **noindex** (썸씬 1,920페이지 색인 방지). canonical → `/tools/beauty-dna/`

### 병합 규칙 (전부 결정적 조립, LLM 불필요)

| 메이크업 단계 | 구동 축 |
|---|---|
| 베이스(파운데이션 호수·피니시) | 퍼스널컬러 |
| 립(계열·마감) | 퍼스널컬러 + MBTI 강도 |
| 아이(팔레트·강도) | 퍼스널컬러 + MBTI |
| 블러셔(색·위치) | 퍼스널컬러 + 얼굴형 |
| 컨투어·하이라이트 | 얼굴형 |
| 헤어(무드·볼륨) | MBTI + 얼굴형 |
| 향(시그니처) | 향수 유형 |
| 페르소나 한 줄 | 4축 조합 룩업(P2에서 선택적 LLM 고도화) |

제품 그리드: `src/lib/recommendations/{personal-color,face-shape,makeup-mbti,perfume-type}.ts` 의
`ProductRec[]` 4개 맵을 병합 · 카테고리 중복 제거 · `RegionToggle`(Korea ⇄ Global) · `ProductGridCard` 렌더.

---

## 4. 페이지 레이아웃 (`/tools/beauty-dna/`)

### 진입점
- 무료도구 허브(`ToolsHub`): 5번째 카드 + 진행 링 `0/4 … 4/4`
- 4개 도구 결과 페이지 하단: `3/4 완료 — 향수 유형만 하면 완성` 스트립 + CTA
- 생성 후: `/analysis/` "이 룩, 내 얼굴에 직접" 배너

### 상태 A — 미완성 (예: 2/4)
- 헤드라인 + 부제 + 진행 링
- 4개 슬롯 카드: 완료 = 유형칩 + 무드컷 썸네일 / 미완 = `[진단하기 →]`
- 하단: "나만의 메이크업 찾았다란?" 개념 롱폼 + FAQ (SEO)

### 상태 B — 4/4 완성, 생성 전
1. 통합 리드아웃 — `[가을웜][하트형][우디][ENFP]` 4칩 + 페르소나 한 줄
2. ★ **나에게 딱 맞는 제품** `[Korea ⇄ Global]` — 먼저, 크게 (수익)
3. 추천 메이크업 루틴 — 단계 카드, 각 단계에 근거 명시
4. 이 조합 읽는 법 — 4 미니카드
5. `━━ [ 내 메이크업 모델 만들기 · 무료 1회 ] ━━` 히어로 CTA (로그인 + 크레딧 게이트)
6. "이 룩, 내 얼굴에 직접" → `/analysis/`

### 상태 C — 생성 후
- 상단: 생성된 모델 이미지(3:4 풀블리드) + `일러스트 · 실제 인물 아님 · 유형 조합 예시` 라벨
- `ShareBar` (`?c=` 공유 URL)
- 그 아래 B의 2~6 동일
- `RelatedTools` · 다시 진단

무로그인으로도 1~4(리드아웃·제품·루틴)는 `localStorage` 로 렌더. **생성만 로그인 필요.**

---

## 5. 빌드 순서

| 단계 | 범위 | 배포 가치 |
|---|---|---|
| **P0** | `useBeautyDna` 훅 · 4개 결과 페이지 저장 · `/tools/beauty-dna/` 상태 A·B · 병합 루틴 · 병합 제품 그리드 · 허브 5번째 카드 · 크로스툴 스트립 | **이미지 없이** 통합 해석 + 루틴 + 쇼핑(수익) + 완성 루프가 바로 나감 |
| **P1** | 베이스 얼굴 5종 사전 제작 커밋 · 4종 코드 → 메이크업 파라미터 변환 · 생성 버튼(크레딧 게이트) · 상태 C · `?c=` 공유 URL | "내 DNA를 입은 모델" 완성 |
| **P2** | EN 미러 · 공유 카드 OG 이미지 렌더러(`gen-og-image` 패턴) · 페르소나 문단 LLM 고도화 · 홈 진입점 | 글로벌 + 바이럴 |
| **P3(선택)** | Supabase 크로스디바이스 동기화 · 참여도가 정당화하면 향수 조명/의상까지 반영하는 풀 온디맨드 렌더(KV 캐시 + fail-closed 가드) | 확장 |

---

## 6. 정직성 체크리스트

- 생성 모델 = `일러스트 · 실제 인물 아님 · 유형 조합 예시` 라벨. 사용자 얼굴 아님, 특정 실존 인물 아님
- 제품: 가짜 평점·가격 금지(기존 규칙). `searchKeywords` ≤5단어 + 제품속성만 → `npm run check:keywords` 통과
- "AI가 당신 얼굴을 생성" 류 문구 금지 → 유형 조합 아키타입 합성
- 접히는 콘텐츠는 `{open && …}` 금지, `<details>` 사용 (크롤러 노출 유지)
- 타입체크는 `tsc -b` (이 리포에서 `tsc --noEmit` 무효)

---

## 7. 미결 / 다음 세션 확인거리

- P0 파일별 diff 계획 (합의 후 착수)
- 페르소나 한 줄: v1 룩업 테이블 문구 초안 (4축 조합)
- 베이스 얼굴 5종 프롬프트 5개 작성 (P1)
- 허브 진행 링 UI 스펙 (`ToolsHub`)
