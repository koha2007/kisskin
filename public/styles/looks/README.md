# 룩 카드 비포/애프터

`scripts/gen-look-models.mjs` 산출물. 비포=Imagen 민낯 모델, 애프터=비포를 라이브와 동일한 gpt-image-2 + promptWholeFace() 로 편집한 실제 결과.
재생성: `set -a && . ./.dev.vars && set +a && node scripts/gen-look-models.mjs --force`
